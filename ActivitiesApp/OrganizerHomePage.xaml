<Page x:Class="ActivitiesApp.Pages.OrganizerHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Организатор"
      Background="{StaticResource AppWhite}"
      FontFamily="Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource AppAccent}">
            <Grid Margin="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Панель организатора"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="Выход"
                        Width="120"
                        Click="BtnLogout_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="{StaticResource AppSoft}"
                    CornerRadius="16"
                    Padding="16"
                    Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="White"
                            CornerRadius="12"
                            Width="100"
                            Height="100"
                            VerticalAlignment="Center">
                        <Image x:Name="ImgPhoto"
                               Stretch="UniformToFill"/>
                    </Border>

                    <StackPanel Grid.Column="1"
                                Margin="16,0,0,0"
                                VerticalAlignment="Center">
                        <TextBlock x:Name="TbGreeting"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="Добро пожаловать в панель управления"
                                   Opacity="0.7"
                                   Margin="0,6,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="{StaticResource AppSoft}"
                        CornerRadius="16"
                        Padding="20"
                        Margin="0,0,16,0"
                        Cursor="Hand">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="👤"
                                   FontSize="28"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Профиль"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0"/>
                        <Button Content="Открыть"
                                Margin="0,14,0,0"
                                Click="BtnProfile_Click"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1"
                        Background="{StaticResource AppSoft}"
                        CornerRadius="16"
                        Padding="20"
                        Margin="0,0,16,0"
                        Cursor="Hand">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="📅"
                                   FontSize="28"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Мероприятия"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0"/>
                        <Button Content="Управлять"
                                Margin="0,14,0,0"
                                Click="BtnEvents_Click"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2"
                        Background="{StaticResource AppSoft}"
                        CornerRadius="16"
                        Padding="20"
                        Cursor="Hand">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="🧩"
                                   FontSize="28"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Активности"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0"/>
                        <Button Content="Перейти"
                                Margin="0,14,0,0"
                                Click="BtnActivities_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
